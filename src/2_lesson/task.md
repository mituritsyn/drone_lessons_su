# Написать следующую логику:
если нажата кнопка, диод моргает с периодом 100 мс, иначе - диод горит постоянно.

# Теория для выполнения
проверка условия выполняется оператором if:

```cpp
if(условие){
    //выполняем код, если условие истинно
    } else {
    //выполняем код, если условие ложно
}
```

Как это используется, можете подсмотреть в [1_lesson.ino](../1_lesson/1_lesson.ino)